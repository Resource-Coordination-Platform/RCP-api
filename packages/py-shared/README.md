# Shared Python package (envelope model, outbox helpers, JWKS auth).
# The IAM and Logistics services currently vendor these ~80 lines each;
# extract here and publish as a wheel once the API stabilises.
