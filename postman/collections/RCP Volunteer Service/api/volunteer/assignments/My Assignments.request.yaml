$kind: http-request
url: "{{baseUrl}}/api/volunteer/assignments"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/My Assignments.resources/examples
order: 2000
