$kind: http-request
name: List Districts
description: Returns the static district adjacency map used for broadcast radius calculations.
url: '{{gatewayUrl}}/api/volunteer/events/districts'
method: GET
headers:
  - key: Accept
    value: application/json
auth:
  type: noauth
order: 1000
