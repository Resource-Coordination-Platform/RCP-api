$kind: http-request
name: List Events
description: List all disaster events for the authenticated tenant.
url: '{{gatewayUrl}}/api/volunteer/events'
method: GET
headers:
  - key: Accept
    value: application/json
order: 3000
